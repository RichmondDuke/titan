name: windows-llvm15-triton-build-from-source

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:

  # ────────────────────────────────────────────────────────────────
  # Job 1: Linux 上用 clang-15 编译 intrinsics → vmprotect*.ll
  # ────────────────────────────────────────────────────────────────
  build-intrinsics:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install clang-15
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y clang-15

      - name: Build vmprotect64.ll and vmprotect32.ll
        working-directory: intrinsics
        run: |
          FLAGS=(
            -std=c++17 -O3
            -Iremill/include
            -Wno-gnu-inline-cpp-without-extern
            -fno-discard-value-names
            -fstrict-aliasing
            -fno-slp-vectorize
            -mllvm -enable-tbaa=true
            -emit-llvm -S
          )
          clang++-15 "${FLAGS[@]}" -DADDRESS_SIZE_BITS=64 -m64 main.cpp -o vmprotect64.ll
          clang++-15 "${FLAGS[@]}" -DADDRESS_SIZE_BITS=32 -m32 main.cpp -o vmprotect32.ll
          echo "=== built ==="
          ls -lh vmprotect*.ll

      - name: Upload intrinsics artifact
        uses: actions/upload-artifact@v4
        with:
          name: intrinsics-ll
          path: |
            intrinsics/vmprotect64.ll
            intrinsics/vmprotect32.ll

  # ────────────────────────────────────────────────────────────────
  # Job 2: Windows 上构建 titan.exe + 收集 DLL
  # ────────────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    needs: build-intrinsics

    env:
      BUILD_DIR: build
      Z3_VERSION: "4.12.2"
      CAPSTONE_VERSION: "5.0.1"
      TRITON_GIT_REF: "master"

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download intrinsics (.ll files)
        uses: actions/download-artifact@v4
        with:
          name: intrinsics-ll
          path: intrinsics/

      - name: Verify .ll files exist
        shell: pwsh
        run: |
          foreach ($f in @("intrinsics\vmprotect64.ll","intrinsics\vmprotect32.ll")) {
            if (!(Test-Path $f)) { throw "$f not found" }
            Write-Host "OK: $f  ($((Get-Item $f).Length) bytes)"
          }

      - name: Setup MSVC Dev Cmd
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup Miniconda (conda-forge)
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          activate-environment: llvm15
          python-version: "3.11"
          channels: conda-forge
          channel-priority: strict

      - name: Set TRITON_INSTALL_DIR
        shell: pwsh
        run: |
          "TRITON_INSTALL_DIR=$env:RUNNER_TEMP\triton-install" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install LLVM 15 dev (conda-forge) + deps
        shell: bash -l {0}
        run: |
          set -euo pipefail
          conda install -y "llvmdev=15.*" "lld=15.*" zlib libxml2 cmake ninja
          echo "$CONDA_PREFIX/Library/bin" >> "$GITHUB_PATH"
          LLVM_DIR="$CONDA_PREFIX/Library/lib/cmake/llvm"
          test -d "$LLVM_DIR"
          echo "LLVM_DIR=$LLVM_DIR" >> "$GITHUB_ENV"

      - name: Setup Python (for Conan)
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Conan 2
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install "conan>=2.0,<3.0"
          conan profile detect --force

      - name: Conan install deps for titan
        shell: pwsh
        run: |
          $jsonPath = Join-Path $env:RUNNER_TEMP "conan-titan-deps.json"
          conan install . `
            -of "$env:BUILD_DIR\conan" `
            -s build_type=Release `
            -s compiler.cppstd=20 `
            -g CMakeDeps `
            -g CMakeToolchain `
            -b missing `
            --format=json | Out-File -FilePath $jsonPath -Encoding utf8

          $j = Get-Content $jsonPath -Raw | ConvertFrom-Json
          if (-not $j.graph -or -not $j.graph.nodes) { throw "Unexpected Conan JSON" }

          function Get-PkgFolder([string]$prefix) {
            foreach ($prop in $j.graph.nodes.PSObject.Properties) {
              $node = $prop.Value
              if ($node -and $node.ref -and ($node.ref -like "$prefix*")) {
                if ($node.package_folder)                             { return $node.package_folder }
                if ($node.packageFolder)                              { return $node.packageFolder }
                if ($node.folders -and $node.folders.package_folder)  { return $node.folders.package_folder }
                if ($node.folders -and $node.folders.package)         { return $node.folders.package }
              }
            }
            return $null
          }

          $boostFolder = Get-PkgFolder "boost/"
          if (-not $boostFolder) { throw "Failed to resolve boost package folder" }
          $boostInc = Join-Path $boostFolder "include"
          "BOOST_ROOT=$boostFolder"     | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "BOOST_INCLUDE_DIR=$boostInc" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install Triton deps (Z3 + Capstone)
        shell: pwsh
        run: |
          $jsonPath = Join-Path $env:RUNNER_TEMP "conan-triton-deps.json"
          conan install `
            --requires="z3/${{ env.Z3_VERSION }}" `
            --requires="capstone/${{ env.CAPSTONE_VERSION }}" `
            -s build_type=Release `
            -b missing `
            --format=json | Out-File -FilePath $jsonPath -Encoding utf8

          $j = Get-Content $jsonPath -Raw | ConvertFrom-Json
          function Get-PackageFolder([string]$prefix) {
            foreach ($prop in $j.graph.nodes.PSObject.Properties) {
              $node = $prop.Value
              if ($node -and $node.ref -and ($node.ref -like "$prefix*")) {
                if ($node.package_folder) { return $node.package_folder }
                if ($node.packageFolder)  { return $node.packageFolder }
                if ($node.folders -and $node.folders.package_folder) { return $node.folders.package_folder }
                if ($node.folders -and $node.folders.package)        { return $node.folders.package }
              }
            }
            return $null
          }

          $z3Folder  = Get-PackageFolder "z3/"
          $z3Inc     = Join-Path $z3Folder "include"
          $z3LibDir  = Join-Path $z3Folder "lib"
          $z3Lib     = Get-ChildItem $z3LibDir -Filter "*.lib" | Where-Object { $_.Name -match 'z3' } | Select-Object -First 1
          "Z3_INCLUDE_DIR=$z3Inc"         | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "Z3_LIBRARY=$($z3Lib.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "Z3_BIN_DIR=$(Join-Path $z3Folder 'bin')" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

          $capFolder = Get-PackageFolder "capstone/"
          $capInc    = Join-Path $capFolder "include"
          $capLibDir = Join-Path $capFolder "lib"
          $capLib    = Get-ChildItem $capLibDir -Filter "*.lib" | Where-Object { $_.Name -match 'capstone|cs' } | Select-Object -First 1
          "CAPSTONE_INCLUDE_DIR=$capInc"         | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "CAPSTONE_LIBRARY=$($capLib.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "CAPSTONE_BIN_DIR=$(Join-Path $capFolder 'bin')" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Clone Triton source
        shell: pwsh
        run: |
          if (Test-Path "_deps\triton-src") { Remove-Item -Recurse -Force "_deps\triton-src" }
          git clone https://github.com/JonathanSalwan/Triton "_deps\triton-src"
          git -C "_deps\triton-src" checkout "${{ env.TRITON_GIT_REF }}"

      - name: Patch Triton source
        shell: python
        run: |
          import re, pathlib, sys
          lib_cml = pathlib.Path("_deps/triton-src/src/libtriton/CMakeLists.txt")
          text = lib_cml.read_text(encoding="utf-8")
          lines = text.splitlines(keepends=True)
          result, skip_depth = [], 0
          for line in lines:
              stripped = line.strip()
              if skip_depth == 0:
                  if re.match(r'if\s*\(\s*PYTHON_BINDINGS\s*\)', stripped, re.IGNORECASE):
                      skip_depth = 1; continue
                  result.append(line)
              else:
                  if re.match(r'if\s*\(', stripped, re.IGNORECASE): skip_depth += 1
                  elif re.match(r'endif\s*\(', stripped, re.IGNORECASE): skip_depth -= 1
          lib_cml.write_text("".join(result), encoding="utf-8")

          top_cml = pathlib.Path("_deps/triton-src/CMakeLists.txt")
          top_text = top_cml.read_text(encoding="utf-8")
          inject = (
              "\n# --- CI PATCH ---\n"
              'set(PYTHON_BINDINGS OFF CACHE BOOL "" FORCE)\n'
              'set(WITH_PYTHON_BINDINGS OFF CACHE BOOL "" FORCE)\n'
              'set(BUILD_SHARED_LIBS OFF CACHE BOOL "" FORCE)\n'
              "# ----------------\n"
          )
          top_text = re.sub(r'(cmake_minimum_required\s*\([^)]*\))', r'\1' + inject, top_text)
          top_cml.write_text(top_text, encoding="utf-8")
          sys.exit(0)

      - name: Configure Triton
        shell: pwsh
        run: |
          cmake -S "_deps\triton-src" -B "_deps\triton-build" -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_INSTALL_PREFIX="$env:TRITON_INSTALL_DIR" `
            -DLLVM_INTERFACE=ON `
            -DLLVM_DIR="$env:LLVM_DIR" `
            -DZ3_INCLUDE_DIR="$env:Z3_INCLUDE_DIR" `
            -DZ3_LIBRARY="$env:Z3_LIBRARY" `
            -DCAPSTONE_INCLUDE_DIR="$env:CAPSTONE_INCLUDE_DIR" `
            -DCAPSTONE_LIBRARY="$env:CAPSTONE_LIBRARY" `
            -DBOOST_ROOT="$env:BOOST_ROOT" `
            -DBoost_NO_SYSTEM_PATHS=ON `
            -DPYTHON_BINDINGS=OFF `
            -DWITH_PYTHON_BINDINGS=OFF `
            -DBUILD_SHARED_LIBS=OFF `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl

      - name: Build & install Triton
        shell: pwsh
        run: |
          cmake --build "_deps\triton-build" --config Release --parallel
          if ($LASTEXITCODE -ne 0) { throw "Triton build failed" }
          cmake --install "_deps\triton-build" --config Release
          if ($LASTEXITCODE -ne 0) { throw "Triton install failed" }

      - name: Configure titan
        shell: pwsh
        run: |
          $toolchain = Get-ChildItem -Path "$env:BUILD_DIR\conan" -Filter "conan_toolchain.cmake" -Recurse | Select-Object -First 1
          $prefixPath = "$($toolchain.DirectoryName);$env:CONDA_PREFIX\Library;$env:TRITON_INSTALL_DIR;$env:BOOST_ROOT"
          cmake -S . -B "$env:BUILD_DIR" -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_TOOLCHAIN_FILE="$toolchain" `
            -DCMAKE_PREFIX_PATH="$prefixPath" `
            -DLLVM_DIR="$env:LLVM_DIR" `
            -Dtriton_DIR="$env:TRITON_INSTALL_DIR\lib\cmake\triton" `
            -DTRITON_INCLUDE_DIR="$env:TRITON_INSTALL_DIR\include" `
            -DBOOST_INCLUDE_DIR="$env:BOOST_INCLUDE_DIR" `
            -DTITAN_BUILD_INTRINSICS=OFF `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl

      - name: Build titan.exe
        shell: pwsh
        run: |
          cmake --build "$env:BUILD_DIR" --config Release --target titan --parallel

      - name: Collect runtime DLLs
        shell: pwsh
        run: |
          $dist = "dist"
          New-Item -ItemType Directory -Force -Path $dist | Out-Null

          $exe = if (Test-Path "build\Release\titan.exe") { "build\Release\titan.exe" } else { "build\titan.exe" }
          Copy-Item $exe $dist

          # .ll 文件（已由 download-artifact 放到 intrinsics/）
          Copy-Item "intrinsics\vmprotect64.ll" $dist
          Copy-Item "intrinsics\vmprotect32.ll" $dist

          # 用 dumpbin 收集运行时 DLL
          $needed = dumpbin /dependents $exe |
            Select-String '^\s+\S+\.dll' |
            ForEach-Object { $_.ToString().Trim().ToLower() }

          $searchDirs = @("$env:Z3_BIN_DIR","$env:CAPSTONE_BIN_DIR","$env:CONDA_PREFIX\Library\bin") |
            Where-Object { $_ -and (Test-Path $_) }

          foreach ($dll in $needed) {
            foreach ($dir in $searchDirs) {
              $candidate = Join-Path $dir $dll
              if (Test-Path $candidate) {
                Write-Host "Copying $candidate"
                Copy-Item $candidate $dist -Force
                break
              }
            }
          }

          Write-Host "=== dist ==="
          Get-ChildItem $dist | ForEach-Object { Write-Host "  $($_.Name)" }

      - name: Upload final artifact
        uses: actions/upload-artifact@v4
        with:
          name: titan-windows-release
          path: dist/
